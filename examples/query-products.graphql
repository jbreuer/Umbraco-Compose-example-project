query GetAllProducts {
  products {
    items {
      ... on Product {
        id
        name
        sku
        description
        price
        currency
        stockQuantity
        isActive
      }
    }
  }
}
