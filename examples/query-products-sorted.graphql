query GetProductsSortedByPrice {
  products(orderBy: [{ 
    product: {
      price: ASC
    }
  }]) {
    items {
      ... on Product {
        id
        name
        price
        currency
      }
    }
  }
}
