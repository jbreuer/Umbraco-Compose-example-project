query GetArticlesSorted {
  articles(orderBy: [{ 
    article: {
      publishedDate: DESC
    }
  }]) {
    items {
      ... on Article {
        id
        title
        publishedDate
        status
      }
    }
  }
}
